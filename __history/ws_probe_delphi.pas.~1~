program ws_probe_delphi;

{$APPTYPE CONSOLE}

uses
  System.SysUtils,
  BenchMain in 'src\BenchMain.pas';

begin
  try
    RunFromArgs(ParamStr(1));
  except
    on E: Exception do
    begin
      Writeln('ERROR: ' + E.ClassName + ': ' + E.Message);
      Halt(1);
    end;
  end;
end.

